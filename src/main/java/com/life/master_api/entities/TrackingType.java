package com.life.master_api.entities;

public enum TrackingType {
    BOOLEAN,    // Hábito de tipo sí/no (completado o no)
    QUANTITY    // Hábito con cantidad específica (ej: 8 vasos de agua, 30 minutos de ejercicio)
}